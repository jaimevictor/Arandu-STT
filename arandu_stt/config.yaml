name: Arandu STT
version: "0.1.1"
slug: arandu_stt
description: STT PT-BR local com FastConformer INT8, correção contextual e Wyoming
arch:
  - amd64
init: false
startup: application
boot: auto
stage: experimental
discovery:
  - wyoming
ports:
  "10350/tcp": null
ports_description:
  "10350/tcp": Wyoming STT (opcional para acesso externo)
homeassistant_api: true
homeassistant: "2026.2.0"
backup_exclude:
  - "models*"
  - "hub*"
options:
  threads: 2
  padding_ms: 0
  profile: full
  debug_text: false
  refresh_inventory: true
schema:
  threads: "int(1,8)"
  padding_ms: "list(0|100|200|300)"
  profile: "list(raw|normalize_only|acoustic_only|fuzzy_only|inventory_only|full)"
  debug_text: bool
  refresh_inventory: bool
